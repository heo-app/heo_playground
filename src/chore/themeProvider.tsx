import { createTheme } from '@mui/system';

export const customTheme = createTheme({
  palette: {
    primary: {
      main: '#1976d2',
      contrastText: 'white',
      light: '#fff',
    },
    secondary: {
      main: '#ec6602',
    },
  },
});
